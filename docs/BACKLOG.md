# FiberLog backlog

> Moved out of CLAUDE.md (July 2026) to keep the always-loaded project memory lean. Rough priority order.

0. ✅ ~~**Usability consolidation + Location drill-in**~~ — closed Aug 25 2026 (verified shipped): location-detail drill-in panel (`LocationDetailPanel.jsx` — view-stock / count hand-off / CSV export / bin labels / edit), Inventory header ⋯ Actions menu (`InventoryView.jsx`), SubmissionsQueue status filter dropdown (via the `ReviewQueue` chassis), bulk-assign UI for pull-locations (`bulk_assign_pull_location`), Reports "By consumer" filter (`consumed_by_user_id`), cross-crew assignment warning (`AdminUsersView.jsx`). The original 12-item plan file no longer exists; the only sub-item never verified shipped ("warehouse row buttons grouped") is not worth reopening — the Locations tab was since reworked around the drill-in panel.

0a. ✅ ~~**Rework ReportsView header chrome for mobile**~~ — shipped (commit `125adf9`). Filters now collapse behind a "▾ Filters" toggle; defaults expanded on desktop, collapsed on phone. Status line summarizes active filters when collapsed.

1. **Onboard infrastructure crew** — substantially shipped (May 2026). Sites table + 198-row import (all mapped to a project), InfraCrewApp shell, crew-aware TaskWorkspace tab strip, `TaskSummaryView` for read-only inspection of submitted tasks, `approve_submission` auto-deducts via `sites.project_id` fallback, project buckets backfilled for Wasatch Front + West Mountain, SubmissionsQueue + ReportsView coalesce phase/site, Sites admin (add / edit including move-to-project / decommission / search / task-count + per-site materials and tasks drilldowns) embedded in ProjectManager. Remaining: (a) author real infra kits in AssemblyEditor (Chris's call), (b) onboard more infra users beyond Chad. Stop dual-logging in Sonar for infrastructure work once switched over.

2. ✅ ~~**Task status redesign** (the `is_closed` model)~~ — **fully SHIPPED July 2026** (verified Aug 25 2026: close/reopen UI lives in `ProjectManager.jsx` (:568 `handleToggleClose`, Open/Closed split) + `SubmissionsQueue.jsx` ("Close task — work is done") + `AdminPanel.jsx` bulk reopen; crew/manager rendering filters via the `is_closed`-based predicates in `crew/taskState.js`, shared by all list components). Original design + DB history kept below for reference. Decouple task lifecycle from submission approval:
   - Tasks get an `is_closed` boolean only (manager-controlled)
   - Submissions keep their own `pending → approved → rejected` lifecycle
   - Approving a submission no longer mutates task status
   - Crew can submit multiple passdowns against the same open task (multi-day work)
   - Manager closes tasks explicitly when the work is done
   - Fixes the "approved tasks still show open" bug we kept chasing; the bug was structural, not cosmetic
   - Implementation: add `tasks.is_closed`, `tasks.closed_at`, `tasks.closed_by`. Remove `tasks.status` writes from the submission flow (leave column for now). Update crew + manager rendering to filter by `is_closed`.
   - **DB shipped (July 2026, migration `20260706000000_tasks_is_closed_lifecycle.sql`):** the three columns exist (backfilled `is_closed=true` for existing `approved`/`done` tasks), and `approve_submission` no longer clears the task draft — it only mirrors `status='approved'` (kept as a display column). ~~Still TODO: manager UI to close tasks, and crew/manager rendering to filter by `is_closed` instead of `status`.~~ Both shipped July 2026 (see strike-through header above).
   - **`log_entries.submission_id` shipped** (migration `20260706232824_log_entries_submission_link.sql`) — entries now link to their submission (uuid FK, `ON DELETE CASCADE` — deliberate, so deleting a flagged submission in the flag-fix replace flow takes its entries with it; `entry_parts` chain-cascades). Fixes the same-day-second-passdown clobber: two pending submissions on one session used to share session-scoped entries, so approving each deducted the union. `approve_submission`'s auto-deduct is now submission-scoped with a legacy session fallback (fires only when a submission has zero linked entries). Backfill linked 5 unambiguous single-submission-session entries; 3 in a multi-submission session stay NULL (served by the fallback). New entries must set `submission_id` at submit time (`TaskWorkspace.handleSubmit` / `save_log_entry` path — frontend wiring is the remaining piece).
   - **`work_sessions` now one-per-(user, day, task)** (migration `20260706120000_work_sessions_per_task.sql`) — was one-per-day. Under multi-task days the old `(user_id, session_date)` unique key forced every task onto one session row: `task_id` got overwritten to the latest task and `handleSubmit`'s session-scoped cleanup deleted earlier tasks' pending submissions. Key widened to `(user_id, session_date, task_id)` (unique index, not a constraint now; `task_id` stays nullable — NULLs distinct, crew flows always pass it). `startSession` upserts `onConflict: 'user_id,session_date,task_id'`. **`crew_activity_today` rewritten** to stay one row per crew user: LATERAL to the latest session for the "current" task/status, LATERAL sum for the day's `entry_count`/`footage_total`/`hours_worked` (CrewStatus keys on `user_id` + sums, so multiple session rows would have broken it).

3. ✅ ~~**Field tech Sonar import → project routing**~~ — **UNBLOCKED Aug 14 2026, no work needed.** The premise ("waiting on Sonar polygon/address data") was stale. Sonar's daily field-tech report already carries a `Project` column on fiber jobs, and `sonar_project_phase_map` resolves **53/53** distinct tags = 100% of the 8,289 addresses in the owner's export (last gap `COLDER SPRINGS` → Cold Springs phase, mapped Aug 14). The ~51% of rows with a blank tag are **wireless** installs (Wave LR/Nano/Pico, PowerBeam) that correctly have no fiber project and route by the wireless part policy; only 6 fiber rows in all of Q2 were untagged. **Do not build an address → project lookup table** — cross-checked against the owner's address export, the address book and Sonar's job tag agreed 759/759 with zero disagreements, so it would change no routing decision. Full detail in CLAUDE.md → "Field tech — routing".

4. **Sage Intacct export — prototype SHIPPED** (`SageExportSheet.jsx`, wired into the Inventory Actions strip AND ReportsView's Consumption view "Export to Sage" button, which passes the report's current date range in). Manual button per export, in-browser CSV download — no Edge Function.

    **What exists** (lib layer in `lib/inventory.js`, three-step lifecycle):
    - `getMovementsForSageExport({since, until, includeExported})` — fetches movements joined with part / from-location / to-location / phase→project. Windows by **effective work date** (`occurred_at ?? created_at` via `movementEffectiveDate()`) so a job imported late still lands in the correct Sage period; skips already-exported rows unless the "Include already exported" toggle is on (re-issue a corrected batch).
    - `isExportableMovement(m, {includeStaging})` — the filter. Always excluded: ALL `adjust` movements (count corrections are FiberLog-internal; Sage runs its own physical-inventory reconciliation), ALL `receive` movements (POs are received directly into Sage, which books the purchase from the AP side — exporting it again double-counts it; this also permanently blocks the 660 one-time BoxHero seed rows from being pushed in as purchases), staging↔staging handoffs (truck / group), and warehouse-internal moves (bin↔bin / warehouse↔bin under the same parent). Crew loads + returns (warehouse/bin ↔ truck **or group**) are dropped by default since Sep 2026 — they're staging, not consumption; the **Include crew loads & returns** checkbox opts them back in. Truck/group→region consumption + issue/scrap always export.
    - `buildSageCsv(movements, opts)` — 18 columns: `TRANSACTIONTYPE, DATE, REFERENCENO, LINE, ITEMID, ITEMDESC, QUANTITY, UNIT, PRICE, FROM_WAREHOUSE, TO_WAREHOUSE, TO_BIN, PROJECTID, CLASSID, DEPARTMENTID, VENDORID, MEMO, FIBERLOG_MOVEMENT_ID`. `DATE` = effective work date. `REFERENCENO` = `SUB-<id8>` when submission-linked else `MV-<id8>`. `PROJECTID` = phase's project name, falling back to the job_site bucket name; `CLASSID` = phase name. `VENDORID` is retained for column-layout stability but is **always blank** — vendors only attach to receives, and receives no longer export. FiberLog location/project names are used directly as codes (no mapping table yet).
    - `markMovementsExported(ids, {userId, notes})` — inserts a parent row in `inventory_export_batches`, then stamps every movement's `exported_at` (batch's canonical timestamp) + `export_batch_id` in chunks of 100 ids (larger `.in()` lists blow the gateway URL limit → HTTP 400).
    - Movement-type mapping (`SAGE_TRANSACTION_TYPE`): `transfer`/`return` → Inventory Transfer, `issue` → Inventory Issue, `scrap` → Inventory Adjustment. `receive` (→ Inventory Receipt) and `adjust` (→ Inventory Adjustment) are still in the map as documentation but are never exported — both are filtered above.
    - Shares the **consumption ledger definition** with Reports → Consumption (`getConsumptionLedger` = transfers landing in `job_site` buckets, dated by effective work date) so the two can never disagree.

    **Still open (why it's labeled "prototype" in the UI):**
    1. Sage code mappings — warehouse names and project names are used verbatim; confirm/map them to the owner's actual Sage codes (transaction-type names are Intacct defaults, customizable per company). **Items: DONE Aug 20 2026** — `parts_catalog.sage_id` holds the Sage Item ID and `ITEMID` exports it (SKU fallback, flagged amber in the preview). 490/609 active parts mapped from accounting's workbook; the remaining ~119 are in `imports/sage-ids/review.csv` (fuzzy/ambiguous, owner to approve) + the "No Sage ID" Parts-tab chip.
    2. Bin-source rows leave `FROM_WAREHOUSE` blank (parent warehouse name isn't joined for the from-side yet)
    3. Cadence/automation — currently a manual button; an Edge-Function scheduled export is the "bigger version"
    4. `STATE`/`BASECURR` columns from the original 46-column template spec were dropped from the prototype

5. **Per-line `project_id` on `log_entries`** — schema change for field-tech multi-cost-center allocation. Pending field-tech UI workflow decisions (per-customer vs per-day).

6. **Field tech UI surface** — flatter "today's installs" list with one-tap into per-customer materials log. ~~Blocked by backlog #3.~~ **No longer blocked** (#3 resolved Aug 2026) — this is now a product/workflow decision, not a data dependency. Pairs with #5 (per-line `project_id`).

7. ✅ ~~**Reconcile workflow**~~ — shipped.

8. ✅ ~~**Locations tab UX**~~ — shipped.

9. ✅ ~~**Auto-deduct on submission approval**~~ — shipped as the project-bucket variant.

10. **BoxHero drafts cleanup** — handled manually by owner via Parts tab → Drafts → bulk edit. ~476 placeholder drafts with `unit='ea'`; cable items need `unit='ft'`.

11. ✅ ~~**Receive materials via vendor PO**~~ — shipped (MVP + inline part create/edit). **Bigger version SUPERSEDED Aug 2026:** Create-PO + per-line/partial receive-against-PO shipped (#47/#53), then the whole purchasing UI was parked behind the Admin → Purchasing switch (#54, OFF by owner decision). Remaining "bigger version" ideas (vendor catalog, overdue-PO reports, PO PDF parser) only matter if purchasing is ever turned back on.

12. **Security & DB hygiene from Supabase advisor scan** — RLS rewrite + view/function hardening complete. Five lints remain, all intentional or out-of-band:
    - `tasks_insert` and `tasks_update` are wide-open by design (crew need to create tasks and auto-save `working_counts`; the "Continued from X" handoff depends on any-crew updates). Documented exception.
    - `approve_submission` and `is_staff()` are reachable by `authenticated`, intentionally. `approve_submission` is the manager UI's approval RPC and has an `auth.uid()`+role guard inside. `is_staff()` is called by RLS policies and must be executable in the user's context.
    - ~~Leaked-password protection toggle~~ — gated behind Supabase Pro plan. Not worth a plan upgrade for FiberLog's risk profile.
    - Performance lints (64 of them, all INFO/WARN, none urgent at current scale): 32 unindexed FKs, 24 duplicate permissive policies, 5 unused indexes, 3 `auth_rls_initplan` cases. Revisit if/when query latency becomes noticeable.

    Helper installed: `public.is_staff()` returns true iff `auth.uid()`'s role is `owner` or `manager`. Used by every staff-write RLS policy.

    Migrations applied: `tighten_rls_policies`, `harden_views_functions_execute_grants`, `users_staff_update_policy`.

13. **Scan mode in CrewMovementSheet (Load + Return)** — opt-in `📷 Scan mode` pill at the top of the crew load/return sheet. Toggle on → scan bin QR to set source, scan part QRs to add lines (each scan = +1 qty for that part; long-press or "edit qty" tap to override). Reuses `ScanInput` from cycle counting (handles both USB scanners + phone camera via ZXing). Bigger payoff than it looks: morning loadouts of 8-15 parts collapse from ~3 min of scrolling/typing to ~30 sec of scanning. Adjacent wins for free: same toggle in Return flow + a "📷 Verify" mode on `MyStockView` that scans truck stock against system state. Gating concern: until label coverage is broad (Receive PO already prints QR; legacy stock needs scan-sheet posting), only a fraction of stock is scannable — toggle stays opt-in so the existing picker remains the default.

14. **Per-Sonar-Project pull-location override (contractor routing)** — contractor fiber jobs get entered in Sonar by various in-house users (cparisi, etc.); the materials should source from the existing `Contractor - RNS` truck (a shared trailer) regardless of which user logged the job. Same transaction process as any other fiber job — only the source changes. Implementation: add `pull_location_override_id` (nullable FK → inventory_locations) to `sonar_project_phase_map`. The fiber-jobs import sheet (and asset import sheet, for symmetry) checks the mapping's override before falling back to the consuming user's `default_pull_location_id` or personal truck. Project mapping picker gets a second dropdown ("source override: [— use user's truck —] / [Contractor - RNS] / ..."). Waiting on a real contractor delivery with materials to confirm what Sonar Project value identifies contractor jobs before building — when a real delivery lands and the column reveals a stable value like `Contractor RNS`, this becomes ~30 min of work. Test with a job that actually has materials (zero-material rows correctly produce no movement under current `no-materials` filter).

15. ✅ ~~**Part-first Crew Load mode**~~ — shipped (commit `ca1fffd`). Default is "🔍 Find a part"; toggle to "📍 Pick a location" for the original flow. `getAllStockGrouped({excludeLocationId})` powers the search across active stock; per-sheet-open mode state.

16. ✅ ~~**Submit-is-permanent warning on task submission**~~ — shipped June 2026. An amber inline note in the crew `TaskWorkspace` submit summary sheet (above the Submit/Keep-logging buttons): *"Once you submit, this passdown is final — you can't edit it after."* (i18n key `submitFinalNote`, en/es). Worded about the **submission** being permanent (not the task), so it stays accurate under the #2 `is_closed` multi-submit model.

17. ✅ ~~**Allow loading more than available stock (over-load override).**~~ — resolved July 2026 with the **warn-but-allow** policy (owner's call, from the audit's parked decisions). Crew Load/Return no longer blocks quantities past on-hand: `validate()` in `CrewMovementSheet.jsx` dropped the over-qty rejection; an amber inline warning ("Loading 22 — only 7 on hand. The source will go negative.") appears live under the qty field, `mergeLine`'s cap is gone, and **any over-draw line forces the review/confirm step** (never slips through the single-part fast path) with the line amber-highlighted. Server side needed no change — `record_crew_movement` never enforced availability. Not gated by a toggle: the review step is the guard, negative stock renders red, and the policy can be tightened to a hard cap later by re-adding one line in `validate()` if the counts become fully trusted.

18. ✅ ~~**Default the Hours field on task submission**~~ — already satisfied (verified June 2026). The crew `TaskWorkspace` hours control is a ± tally that initializes to `8` (`useState(8)`, `TaskWorkspace.jsx:56`) and restores from the saved draft when present — never blank. Flat-8 was the recommended lowest-friction default and it's in place. Possible future upgrade (declined for now): remember each crew member's last-entered hours per user.

19. ✅ ~~**Crew "found inventory" → manager-approved request**~~ — shipped (June 2026). Request-and-confirm flow (no direct crew write to stock). Crew tap **"Report found inventory"** in My Stock (`FoundInventorySheet.jsx`) → search the catalog (or add a new part inline, carried as draft fields on the request) → qty + destination warehouse + reason → creates a **pending** `inventory_intake_requests` row; no stock moves. Manager reviews in the Inventory **"Found"** sub-tab (`IntakeRequestsQueue.jsx`, realtime, mirrors SubmissionsQueue) → **Approve** runs `approve_intake_request` (idempotent, staff-guarded): materializes a draft part (`is_active=false`) when there's no `part_id`, books a `receive` movement into the warehouse (trigger updates stock), flips to `approved`; **Reject** (`reject_intake_request`) carries a reason. Draft parts surface in Parts → Drafts for curation (#10). Table + RLS (crew write/read own, staff read/update all) + both RPCs in migration `20260624000000_inventory_intake_requests.sql`. Lib helpers: `createIntakeRequest` / `getIntakeRequests` / `approveIntakeRequest` / `rejectIntakeRequest` in `lib/inventory.js`. Related: #17 (over-draw on the truck side) and #10 (draft cleanup).

20. ✅ ~~**🐛 BUG — `recordMovementsBatch` drops `phase_id` + `consumed_by_user_id`**~~ — fixed June 2026. The batch insert payload in `lib/inventory.js` (`recordMovementsBatch`) now forwards `phase_id` + `consumed_by_user_id`, which `SonarImportSheet.jsx` (~828/830) and `FiberJobsImportSheet.jsx` (~475/476) stamp on each movement for per-cost-center Sage rollups + "by consumer" reporting (#4 / #0). Before the fix every Sonar/fiber-jobs movement wrote NULL for both (evidence at the time: 88 `[sonar…]` movements, only 32 with a non-null `phase_id`). Forward-looking only — pre-fix rows already written with NULL are not backfilled (no reliable source to recover phase/consumer per historical row).

21. **Code-audit follow-ups (June 2026).** Most items SHIPPED; a few intentional holds remain.
    - ✅ **Correctness/convention** — reset-password gate `<6`→`<8` (`AdminUsersView.jsx`); `.subscribe()` wrapped in try/catch in `SubmissionsQueue.jsx` + `CrewStatus.jsx`; `AppContext` `app_settings` channel now uses `nextChannelSuffix()` + try/catch; crew-mode now gated behind `!restricted_to_inventory` in both `App.jsx` and `ManagerApp.jsx`'s `SwitchToCrewButton`.
    - ✅ **Responsive** — `PurchaseRequestsTab.jsx` now collapses to cards under `useIsWide` at narrow widths.
    - ✅ **Legacy removal (bucket B)** — `crew/UserSheet.jsx` + `src/UserPicker.jsx` deleted; orphaned i18n keys (`whoAreYou`/`searchName`/`pinComingSoon`) removed.
    - ✅ **Noise** — debug `console.log`s removed (`SubmissionsQueue`/`ReportsView`); `PartSearch` got `autoComplete="off"`; `InfraCrewApp` optimistic-insert default `'aerial'`→`'task'`; CLAUDE.md chunking note corrected.
    - **KEPT — intentional scaffolding (bucket C):** `clearSonarProjectPhase` / `clearSonarSourceLocation` / `clearFiberValueMap` (clear-siblings of used get/set pairs; relate to #14), `Icon` `ICON_NAMES`, `PartSearch` `filter` prop.
    - ✅ **Bucket D resolved Aug 25 2026:** `getPartsByCategory`, `getPendingSubmissions`, and `ProjectManager`'s `projFiber` state all deleted (zero callers confirmed repo-wide). Still deferred-by-design: the no-op `ThemeToggle` pill (see #23) and `admin.js`'s reliance on the internal `db.supabaseUrl`.

22. **Consolidate the "review queue" pattern (tech debt / usability).** The app has ~6 "provisional record → manager confirms" mechanisms with no shared abstraction; each is bespoke.
    - ✅ **Tier A — DONE July 2026:** `useRealtimeQueue(table, {channelPrefix, onEvent})` in `lib/useRealtimeQueue.js` (subscribe + try/catch + 2s auto-reconnect, onEvent ref'd) and the config-driven `<ReviewQueue>` chassis in `manager/ReviewQueue.jsx` (header/pending badge/status filter/detail overlay + `ReviewActions`/`InitialsAvatar`/`StatusPill` exports). Both queues migrated with line-by-line parity; Submissions' extras (project grouping, archived toggle, parts loading, stat pills, close-task, archive) deliberately stay bespoke — a chassis extra costing >~3 slot props stays in the queue. A third approval flow is now nearly free.
    - **Tier B — later, partial:** cycle-count resolutions (`CountRunReviewSheet`/`approve_count_resolution`) and Sonar pending imports (`getPendingSonarImports`/`discardSonarPendingImport`) share the *approve/reject-with-reason + status* shape but have different chrome — share the data/action layer only, not the list/detail UI.
    - **Tier C — leave distinct (do NOT force together):** Parts → Drafts (`is_active` metadata edit, no queue/reason), Purchase Requests (procurement lifecycle, manager-originated, no approve/reject buttons), Tasks status (the task-side mirror of submissions).
    - **Adjacent workflow win (separable):** kill the **Found→Draft double-handling** — a found-new-part is touched twice (approve the intake, then fix unit/department in Parts → Drafts). Let the manager curate the part (SKU / unit / department, or merge into an existing SKU) inline at found-approval so the draft is "born clean." Relates to #10.
    - Note: only 2 Tier-A queues exist today (borderline rule-of-three), but the "mirrors X" duplication + the steady drip of new approval flows make a shared abstraction pay off forward.

23. ✅ ~~**🐛 BUG — dark-mode toggle does nothing (theme never switches).**~~ — **ROOT-CAUSED + FIXED Aug 25 2026.** The comment above the `[data-theme="dark"]` block in `global.css` contained the text `--orange*/--teal*` — the embedded `*/` **closed the comment early**, the rest of the comment became garbage CSS tokens, and the parser's error recovery consumed everything up to and including the entire `[data-theme="dark"] { … }` block as one invalid rule. The dark palette was never in the parsed stylesheet at all (confirmed via CSSOM inspection: the sheet jumped from `[data-theme="light"]` straight to `*`). That's why every source-level review "verified the CSS correct" — the rule was perfect; the comment above it was the bug. Fix: reworded the comment to avoid the `*/` sequence; toggle un-hidden in both ThemeToggle components (`SignOutConfirm.jsx` + `ManagerApp.jsx`). Verified end-to-end in the running app: click → `<html data-theme="dark">` → slate palette renders (manager Console + login screen screenshots checked) → persists via `fiberlog_dark_mode_v2` → toggles back. Lesson for the file: never put `*/` inside a CSS comment (`grep -n '[a-z]\*/' global.css` is the tripwire). Original investigation kept below for history. Painted a dark-slate palette into `[data-theme="dark"]` (global.css), flipped the default to light, bumped the storage key to `fiberlog_dark_mode_v2`. But clicking the theme toggle never visibly switches to dark — reproduced on phone + PC, in **clean incognito** (extensions off, no cache). **Verified NOT the cause:** the live CSS is correct (`[data-theme="dark"]{…--bg:#0B1120…}` after `:root`, equal specificity so dark wins; `html,body,#root{background:var(--bg)}`), the wiring is correct (`toggleDarkMode` is in the AppContext value, `ThemeToggle onClick={toggleDarkMode}`, and the effect `document.documentElement.setAttribute('data-theme', darkMode?'dark':'light')` runs on `darkMode` change), it's not cache, not an extension. So forcing `data-theme="dark"` *should* turn the page slate — yet a user's `getComputedStyle(body).backgroundColor` came back light (`rgb(251,251,252)`). **The one unconfirmed fact for next time:** does clicking actually flip `<html data-theme>` to `"dark"`? Watch the `<html>` tag in DevTools → Elements while clicking. If it flips but the page stays light → the dark CSS isn't applying for that client (bizarre, given it's verified live) — re-check at runtime. If it doesn't flip → the click handler isn't firing → debug the event/handler in the deployed build. Best to **reproduce locally** (`npm run dev` + a real browser + Elements panel). **Current state:** dark palette CSS is dormant (default light, no effect); the toggle is **hidden** (`return null` at the top of all 3 `ThemeToggle` components — ManagerApp, CrewApp, InfraCrewApp) so users don't see a dead control. **To restore:** delete those `return null` lines once fixed.

24. **Warehouse-as-parent cleanup — (a) ✅ SHIPPED, (b) open.** Part (a) shipped in commit `0612079` ("bin-capable destination pickers in recovery flows") — the location-retire dialog uses `LocationWithBinPicker` with per-warehouse bin drill-in (verified Aug 25 2026); the other recovery flow, site-decommission, was later removed entirely (Aug 2026, accounting-only decommission — field returns own gear recovery), so (a) is fully closed. Part (b) below remains, and aligns with the owner's stated "retire unbinned stock" direction (no timeline). Original finding: Surfaced June 2026 retiring a location. **Bug:** the location-retire picker (`InventoryLocationsTab.jsx` `destOptions`, ~`:779-789`) and the site-decommission picker (`ProjectManager.jsx` ~`:308`, warehouses-only) build their destination list from `getLocations()`, which defaults to **`includeBins:false`** — so they only offer top-level locations, **never bins** (the code comment claiming it includes warehouse bins is wrong — it never merges them). Since stock can sit at warehouse level ("unbinned"), the warehouse was the only pickable target → "moved Main warehouse → Main warehouse." These two **recovery dialogs are the only stock-destination pickers without bin granularity**; every normal movement sheet (Receive PO, Move, Record, Bulk, crew Load) already does a warehouse→bins drill-in via `getBinsForWarehouse()` / `getLocations({includeBins:true})`.
    - **(a) Quick fix (do first, ships alone):** give both recovery pickers the same bin-capable (aisle-grouped) destination picker the other sheets use, so retired/decommissioned stock can move into a specific bin (or another warehouse), not just the parent.
    - **(b) Larger model change (separate, migration-bearing):** make a warehouse a **parent label only**, not a stock-holding location — add a default bin per warehouse (e.g. "Unbinned"/"Floor"), **migrate all existing warehouse-level `inventory_stock` into it**, and retarget every picker that allows a warehouse as a direct destination (Receive PO, Move, Record, Bulk, crew Load, both recovery flows) to target a bin. The existing warehouse rollup view becomes the natural summary; single-level bin nesting already enforced. Kills the circular retire-to-warehouse behavior at the root.

25. **In-app "Fiber Log List" importer** — a reusable version of the June 2026 name-keyed reconcile (see `imports/reconcile-6-19/`). Parse a name+qty list (no SKU), normalized name-match against `parts_catalog` (`lower()` + `regexp_replace('[^a-z0-9]+',' ')` + trim — got 98.7% on the 6/19 list), with a disambiguation step for ambiguous (name → 2+ SKUs) + unmatched (typos), reconcile WH/Contractor counts to target (delta = target − current, one-sided `adjust`), and run the existing orphan sweep — all in-app with a preview. The first cleanup was done script-assisted via MCP; build this when list-updates become recurring. **Key gotcha it must handle:** the catalog has non-material SKUs (tools `…-CLST`) and a per-physical-reel fiber system (`R-<ct>-<code>`) that a materials list omits — so "deactivate everything not on the list" is category-sensitive, not blanket.

26. **Field-material dispersal import** — the 6/19 list's `Field Qty` (40,512 units / 82 parts) is a pooled aggregate with no single destination. Chris to provide a dispersal sheet mapping each part's field qty → specific crew member/location; then name/SKU-match and post `adjust` movements to those locations. Contractor column (5,026 / 5 parts) can post straight to the `Contractor - RNS` group. Blocked on the dispersal sheet.

27. **Catalog duplicate + naming cleanup (surfaced by 6/19 reconcile).** Real duplicate SKUs exist: leading-zero twins (`65910`/`0065910`, `311255`/`0311255`, `16282`/`0016282`, `409664`/`0409664`), `-ALB` twins (`600-100032`/`600-100032-ALB`), spacing twins (`MST-2P-50ft`/`MST - 2P 50ft`). Some hold stock and need a **merge** (move stock to canonical → retire twin) — notably `J Type Drive Hook 7/16 x 4-3/4` (a `(920)`-less twin `600-100094` holding 1,097 units) and the per-reel fiber SKUs (5 reels, 41.6k ft in group buckets) if the reel system is ever collapsed into aggregate "Fiber reel - X ct" SKUs. The 6 zero-stock leading-zero twins were already retired June 2026; the stock-holding merges remain.

--- *Items 28–36 from the July 6 2026 full audit (`reports/20260706-audit1/full-audit.md` — evidence + file refs live there).* ---

28. ✅ ~~**🔴 `ensure_crew_truck` trigger missing merged crew types.**~~ — fixed July 2026 (migration `ensure_crew_truck_merged_crew_types`). IN-list gained `fiber_construction` + `field_service`; sweep found no other stale crew_type lists (`record_crew_movement` looks up restrictions dynamically, `replace_assembly` passes the assembly's work-logging crew_type through, both CHECKs already include the merged values). No backfill needed — zero active merged-type users were truck-less. Residual gap (report-only): `trg_ensure_crew_truck` fires on `INSERT OR UPDATE OF role, is_active` — setting crew_type alone on an existing user still won't create a truck.

29. ✅ ~~**🔴 Stock tab under-reports + 2 broken searches.**~~ — fixed July 7 2026. Root cause was NOT the rollup: **PostgREST silently caps un-paginated selects at 1,000 rows** and `inventory_stock` crossed it (~1,389 rows); truncation follows heap order so recently-touched rows vanished first (the "80 vs 480" part's 400-unit row sat past the cap, and the "lag" parts never reached the client — which is why search looked broken). Fix: exported pager `fetchAllRows(buildQuery, {maxRows})` in `lib/inventory.js`; ALL >1k-capable stock reads routed through it (getStockSummary, getAllStockGrouped, getStockCountsByLocation, getStockTotalsByPart, getStockForWarehouseTree, exportLocationStockCSV, getStockForAudit, getStockRowsForParts, **ReconcileSheet's live-stock resolve** — that one feeds adjust movements, so a truncated row read as 0-on-hand and would have posted inflated adjustments). Stock-tab search also tokenized. **Rule: any read that can return >1,000 rows must page via `fetchAllRows` with a stable `.order()` — non-negotiable for anything feeding movement writes.** Defect (c) (bin-rollup search "never filters") was not reproducible in source — likely a QA browser-tab artifact; re-report if a human reproduces it.

30. ✅ ~~**🔴 `deactivate_location_with_recovery` RPC still owner-only.**~~ — fixed July 2026 (migration `recovery_rpcs_staff_guard`). Owner-only role check swapped for `is_staff()` (owner OR manager) in BOTH `deactivate_location_with_recovery` and `decommission_site_with_recovery` (the sibling had the identical owner-only guard). Grants preserved; smoke-tested a manager-JWT retire with stock recovery (rolled back).

31. ✅ ~~**🔴 Multi-word part search returns 0 everywhere.**~~ — fixed July 7 2026. `searchPartsCatalog` tokenizes on whitespace and ANDs tokens via chained `.ilike` per column (all tokens must hit the SAME column); covers every search funnel (PartSearch, Found-inventory, cycle-count picker, manager flows). `CrewMovementSheet`'s two client filters use a `matchesAllTokens(query, fields)` helper (tokens may match ACROSS fields — deliberate, documented asymmetry). `InventoryStockTab`'s search tokenized under #29.

32. ✅ ~~**🔴 Cycle-count end-run 400 with zero UI feedback.**~~ — fixed July 7 2026. The End-run confirm now derives `blockedBins` (unsubmitted sessions beyond the active one), lists them by name in an amber warning, and disables End while any exist — mirroring the server rule so the 400 is unreachable from the screen; a 22023 catch (race fallback) refetches sessions, toasts the bin names, and reopens the dialog. Bonus fixes in the same pass: part-picker no longer auto-saves qty=1 on selection (opens the qty sheet; Cancel writes nothing; scans keep instant +1), failed part-searches say so instead of "No matches" (and suppress the create-as-new offer), CountTab load failures toast.

33. ✅ ~~**🔴 Users-admin role-flip footgun.**~~ — fixed July 7 2026. `CrewPermissions`/`LoadDestinations` sections now gate on the PERSISTED user (`user.role` or a field `crew_type`) so working managers reach them without flipping access type; any role change on save pops an explicit confirm (with a "loses manager portal" warning on demotions); existing users' NULL crew_type stays NULL on unrelated edits (`''` default, mapped to null by accessTypeToFields; new users still default fiber_construction). (`AdminUsersView.jsx`.)

34. ✅ ~~**Crew passdown visibility on open tasks + `getTaskSummary` rewrite.**~~ — shipped July 7 2026. (a) TaskWorkspace shows a tappable "N passdowns submitted · Xh · latest HH:MM" strip (banner-muted, above the tabs) opening a read-only history overlay; the shared `PassdownList` component (`crew/PassdownList.jsx`) renders passdown cards for BOTH the overlay and `TaskSummaryView` (which now lists ALL passdowns, not just the latest). (b) `getTaskSummary` rewritten: server-filtered via `work_sessions!inner(task_id)`, no limit-20 window, archived included; returns `{submissions:[each with parts+notes], totalHours}`; per-submission parts via `log_entries.submission_id` with a single-owner-session fallback for legacy unlinked rows ("better absent than double-counted"). (c) Flag banner shows the flagged passdown's own hours + first 4 parts ("What you submitted: …"), user-scoped so a coworker's flagged passdown on a shared task can't masquerade as yours. Known deferral: `PassdownList` strings are English-only (goes with #35).

35. **i18n: crew language — mostly SHIPPED, small remainder.** (Rewritten Aug 25 2026 — the original entry predated the July i18n wave and was stale.) Shipped: self-service EN/Español toggle in the sign-out sheet (`SignOutConfirm.jsx`, persists per-device via `localStorage.fiberlog_lang`, deliberately kept across logouts) plus a login-screen toggle; full `t()` wiring in `MyStockView`, `CrewMovementSheet`, `FoundInventorySheet`, `PassdownList`, `TaskWorkspace`, `SetNewPassword`, TaskList job-type pills (`labelKey`); `lib/i18n.js` at ~313 keys. **Remaining:** (a) `CrewApp.jsx` itself has zero i18n wiring (the only crew-shell file without it); (b) date formatting is not locale-aware — `fmtWhen`/`fmtShortDateTime` in `lib/format.js` use browser locale or hardcoded `en-US`.

36. ✅ ~~**Consolidation program**~~ — SHIPPED July 2026 across two waves: (1) CSV-import shared layer (`importShared.jsx` + `lib/useCsvImport.js`, Sonar −370 / FiberJobs −337 LOC; Sonar/FiberJobs applies kept deliberately ATOMIC after review caught a chunked-retry double-book hazard); (2) queue consolidation → see #22 ✅; (3) QR label merge (`shared/QrLabelSheet.jsx`, AisleSign multi-page print bug fixed); (4) style tokens (`lib/format.js` + `manager/chrome.jsx`: chipStyle/cardSurface/LoadingBlock/EmptyState, zero visual change); (5) crew chrome (`crew/taskState.js` + `crew/SignOutConfirm.jsx`, ProjectList/PhaseList/SitesList progress predicates fixed status→is_closed, BulkMoveSheet adopted LocationWithBinPicker + chunked batch); test suite re-pointed (dead `calculations.js` deleted; **50 tests** now cover validateMovement's endpoint matrix, buildSageCsv + exclusions, extractMarkerKeys, mergePartsById, matchesAllTokens); DB tidy (FK indexes, duplicate index drop, anon+PUBLIC revokes). Anti-goals honored (no unified MovementSheet/CrewShell/ResponsiveTable/import-resolver merge; `lib/inventory.js` barrel-split still deferred). Original ranked list for reference: (1) CSV-import shared layer, ~550–700 LOC dup between Sonar/FiberJobs/Inventory importers, incl. byte-identical Section/MappingRow + webhook panels; (2) #22 queue consolidation, now measured (~80% of IntakeRequestsQueue is copy) — `useRealtimeQueue` hook first; (3) QR label sheets (Sku↔Bin 85% identical) + port AisleSignSheet off the broken multi-page print technique; (4) style/format token dedup (chipStyle ×6, fmtWhen ×8, STATUS_COLORS ×3, location-icon maps ×4); (5) crew-shell chrome + shared `crew/taskState.js` predicates — **fixes a real bug**: `SitesList`/`ProjectList`/`PhaseList` still count `status IN (approved,done)` as complete, so approved-but-open tasks read 100% done in cards while sitting in Active one level deeper; also BulkMoveSheet re-implements LocationWithBinPicker byte-for-byte — adopt the shared one. Anti-goals (measured, don't do): unified MovementSheet, unified CrewShell, generic ResponsiveTable, unified import resolvers. Adjacent quick wins: chunk Sonar/FiberJobs `recordMovementsBatch` calls (InventoryImport already chunks at 100); ✅ DB tidy shipped July 7 2026 (`20260707100000_db_tidy_indexes_grants.sql`) — 8 FK indexes on the hot submission-flow paths, dropped duplicate `inventory_movements_unexported_idx` (kept `idx_inv_movements_unexported`), revoked anon EXECUTE on the 5 guarded RPCs (record_crew_movement also carried a stray PUBLIC grant, removed; authenticated unaffected); **re-point the test suite** — `calculations.js` (the only tested module, all 34 tests) is unreachable from the app; the real submit math/validateMovement/buildSageCsv have zero coverage. Product decisions — SETTLED July 2026 (owner's calls): **accounting scope keeps NO Approvals access by design** — approvals drive material auto-deduct, an operations decision; `staff_scope='accounting'` = Reports + limited Inventory, full stop (personas/docs updated to match). **Silent-disabled buttons: swept** — crew flows now show inline reasons (Load/Return qty + primary button in `CrewMovementSheet`, task-name hints in `TaskList`/`SiteTaskList`; FoundInventorySheet + submit-day were already fine). **Whitelist UX: badge-and-grey shipped** — restricted parts stay visible in both crew pickers but greyed + "Not loadable for your crew" badge + unpickable (`getCrewTypePartRestrictions` mirror of the RPC check, fail-open; `getStockByLocation` now carries `department`); note the RPC also blocks RETURNING restricted parts — mirrored, not diverged (revisit if a restricted part ever strands on a truck). Cycle-count qty=1 autosave was already fixed under #32. Still open (trivial): stale "Owner only" copy in Admin header.

37. ✅ ~~**Depreciated ("no-value") parts tagging**~~ — SHIPPED Aug 25 2026 exactly per the settled design below: migration `20260825120000_parts_catalog_is_depreciated.sql` (applied live, inert to old bundle); `buildSageCsv` appends `[no-value]` to MEMO (emitted even on null notes — tested, incl. vendor-note back-compat); tag entry points = Parts edit form (amber checkbox card) + NO-VALUE list badge + `Depreciated` CSV column + Receive PO inline-create checkbox; `pickPart`/`asLinePart` carry the flag; amber warn-not-block hint when a flagged line has a unit cost; `getMovementsForSageExport` part embed + PO-picker search cols include `is_depreciated`; SageExportSheet stats row counts `[no-value]` lines. Found-inventory flow deliberately untouched. Known deferral (reviewer note): lines arriving via the PO hand-off (Create-PO → Receive) build `line.part` from PR data without `is_depreciated`, so the no-value badge/cost-hint won't render on them — moot while the #54 Purchasing switch is OFF; wire it when purchasing turns back on. Original design: — used/recovered equipment (e.g., gear from decommissioned sites) must be tracked normally in FiberLog but flagged so accounting doesn't book it as new inventory value. Today it's invisible: recovered gear books as an ordinary valued receive and exports to Sage indistinguishable from new purchases. (Aug 2026: field returns + refurb `-R` twins now cover the recovered-equipment case — site decommission no longer offers in-modal recovery and points at Receive PO → Returned from field — so what's left of this item is the general no-value flag.) Design settled with owner Aug 2026: (a) new `parts_catalog.is_depreciated boolean NOT NULL DEFAULT false`; same-part-both-conditions handled by a separate "(Used)" SKU convention (part-level flag chosen over per-movement tagging — movements are immutable and mixed stock is indistinguishable at consumption time); (b) Sage export **keeps** flagged lines but appends a `[no-value]` marker to the MEMO cell (`buildSageCsv` :2272 — must emit even when `notes` is null) so accounting filters in Sage; PRICE/`isExportableMovement` untouched; (c) tag entry points: Parts admin edit form (`InventoryPartsTab` PartFormSheet + list badge + `Depreciated` CSV column) and Receive PO inline part creation (checkbox → `createPart`, which takes an explicit field set and needs the new param; `pickPart` :382 builds a stripped part object and must carry the flag; amber warn-not-block hint when a flagged line has a unit cost). Found-inventory flow deliberately untouched. Wiring notes: add `is_depreciated` to the `getMovementsForSageExport` part embed (:2008) and to `searchPartsCatalog`'s default cols (`lib/supabase.js` :41 — the PO picker depends on it); `updatePart` passes through unchanged; SageExportSheet stats row shows a count of `[no-value]` lines. Tests: buildSageCsv memo marker with/without existing notes + vendor-regex back-compat. Migration ships before the JS deploy.

38. ✅ ~~**Record movement: visible on-hand + "Counted total" Adjust mode**~~ — SHIPPED Aug 25 2026: (a) every line row shows the muted On-hand summary (top 3 locations by qty + total, amber "No logged stock at any location") for all movement types, from the already-fetched `partLocationsByPart`; (b) Adjust gained the **Counted total** pill — counted-location picker (bin-capable), per-line `sys N` + ±delta/match chips, all-match guard ("Counts match the system — nothing to book"), payloads via the new tested `buildCountedAdjustPayloads` in `lib/inventory.js` with system qtys from `getStockRowsForParts` (keeps negative rows — the designed-around gotcha). Verified live in dev (search → add → counted location → sys/delta/match chips → all-match guard). Original design: — owner report Aug 2026: adjusting a part shows no location quantities anywhere. The data is already in the sheet (`getPartLocations` → `partLocationsByPart`, fetched per added part) — it's just buried in the collapsed To-dropdown's "· N on hand" option text; and Adjust asks for a **delta** while the manager at the shelf thinks in **absolutes**. Design settled with owner: (a) every line row in `RecordMovementSheet` gets a visible muted summary — `On hand: Main Warehouse 8 · Truck — Chad 2 · +1 more (total 11)` (top 3 by qty from the already-fetched data, zero new queries; amber "No logged stock at any location" when empty) — for ALL movement types; (b) Adjust's direction row gains a third pill, **📋 Counted total**: pick the counted location (`LocationWithBinPicker`, bin-level works), each line's qty input becomes "Counted" with `sys: N` + a ±delta preview beside it, submit books per-line one-sided adjusts (`delta>0` → to-only, `<0` → from-only abs, `=0` skipped; all-zero → "Counts match the system — nothing to book") with Reconcile-style notes (` — counted N, system had M`). **Gotcha:** system qtys for the delta must come from a real stock fetch including zero/NEGATIVE rows — `getPartLocations` filters `qty <= 0` (`lib/inventory.js:295`), so an over-drawn location would wrongly read as 0 (reuse/extend the #29 `getStockRowsForParts` paged helper, filtered to the picked location). Extract `buildCountedAdjustPayloads(lines, systemQtyByPartId, locationId, opts)` into `lib/inventory.js` with tests (payloads must pass `validateMovement`'s one-sided adjust rules; missing row = 0, negative-system case, note format). Reconcile + Cycle Count stay the bulk-count tools — this is the lightweight spot-check path with identical `adjust` semantics (never exports to Sage). Doc touch-ups: CLAUDE.md flows table + INVENTORY_TAB/MANAGER_GUIDE Record-movement blurbs.

39. ✅ ~~**🔴 Cycle count: false-zero trap + no post-submit correction.**~~ — **FULLY SHIPPED: DB Aug 6 2026 (migration below) AND UI Aug 6 2026 (commit `4e10b36`, verified live Aug 25 2026: labeled "None" ghost button + pencil edit on Found rows + "Clear count" un-count in `QtyOverrideSheet` + "Reopen" on submitted bins via `reopen_count_session` + corrected pre-submit copy, all in `CountRunScreen.jsx`).** The "UI still pending" language below predates that commit. Original finding + design: Owner report Aug 2026: the "0" (found-none) button on Still-missing rows (`CountRunScreen.jsx:554`, `handleCountZero` :222) is styled like an input flush against Count — one mis-tap silently records `counted_qty=0` (no confirm, no undo), the row jumps to "Found" out of view with a bold 0 whose only edit affordance is an unlabeled whole-row tap (:572), and tapping zeros is the fastest way to un-disable Submit (:648). After **Submit bin** there is NO correction path: no un-count exists (nothing can set `counted_qty` back to NULL), `delete_count_line` refuses expected lines by design, `start_or_resume_count_session` raises `23505` on submitted sessions, and **no reopen RPC exists** — the UI copy at :782 ("unless the run is reopened") is aspirational. Worst case at end-of-run: a false zero's `-expected` variance **auto-pairs with any positive variance in the same warehouse and silently books a real `transfer`** (`status='auto'`, never reaches review); unpaired it becomes a `net_loss` the manager can only approve/discard blind (`getCountRunDetail` never loads `count_lines`). Fix design settled with owner (clear-button + undo): (a) restyle the 0 into an obvious labeled ghost button ("None") that can't read as a field; keep one-tap (fast for empty shelves) but make it reversible — Found rows get a visible edit (pencil) control opening the existing `QtyOverrideSheet`, which gains a "Clear count" action restoring `counted_qty=NULL` (row returns to Still missing; verify `record_count_line` with NULL — the `< 0` guard passes NULL through, but harden the RPC: NULL only valid on existing expected lines); (b) new `reopen_count_session(p_session_id)` RPC (staff-gated: session `submitted` → `in_progress`, only while the run is still `in_progress` — pre-end-run) + a "Reopen" button on submitted bins in `BinsCountedList`, making the :782 copy true; (c) fix that copy either way. Cycle-count RPCs live only in the DB (not in `supabase/migrations/`) — implement via the supabase-migration subagent. **DB side SHIPPED Aug 6 2026** (migration `20260806000000_count_line_unclear_and_reopen_session.sql`, applied live): `record_count_line(p_counted_qty => NULL)` is now a deliberate un-count — restores an existing EXPECTED line to counted_qty NULL / Still-missing; raises 22023 for ad-hoc expected=0 lines (pointing at `delete_count_line`) and for parts with no line (previously the upsert would insert a ghost expected=0/counted=NULL row); non-NULL path unchanged. New `reopen_count_session(p_session_id)` RPC: staff-gated (42501), idempotent, flips `submitted` → `in_progress` + clears `completed_at`, refuses with 22023 unless the parent run is still `in_progress`; deliberately does NOT clear the bin's `last_counted_at` (resubmit re-stamps). Grants match the sibling count RPCs (authenticated + service_role; no anon/PUBLIC). UI (a/b/c) still pending — ships separately. Optional follow-ups noted, not in scope: review-sheet loading `count_lines` so resolutions show counted-vs-expected, and surfacing auto-paired transfers for review. **Workaround until built:** fix a wrong zero BEFORE submitting by tapping the part's row in "Found" (opens the qty sheet); treat Submit bin as final; if a bad bin was submitted, the only options are discard the whole run or discard the resulting net_loss resolution at review (auto-paired transfers can't be unwound from the review UI — counter-book manually via Record movement).

40. ✅ ~~**Crew Load multi-part redesign (owner UX walkthrough Aug 2026).**~~ — **SHIPPED Aug 25 2026** (the settled fix order, on top of the cart/review/i18n work that had landed since this walkthrough): **(1)** by-part search rows now carry **checkboxes** — tick (part, location) rows across searches, a sticky "＋ Add N" bar pushes them all into the cart at qty 1, and **cart quantities are edited inline** (number input per line, live over-draw amber + zero-qty red, submit guard) — kills pains 1–3; **(2)** a part with exactly one stocked location **picks on header tap** (tooltip "Tap to pick — only one location"; the location row stays as the explicit path) — kills pain 5; **(3)** the by-location **Change lock now applies only to RETURN** (shared destination snapshot); LOAD carts are cross-bin, safe because every line snapshots its own source. Fast path (single part → own truck, no Add/confirm), prefill paths, warn-but-allow + forced review, whitelist grey, failed-line retry, backClose dirty-confirm (now includes pending ticks), and en/es (2 new keys) all verified intact — smoke-tested live in crew mode. Remaining nice-to-haves NOT built: recents row (pain 6), batch RPC (pain 7), scan mode (#13). Original walkthrough: The load flow (`CrewMovementSheet.jsx`) is a one-part-at-a-time wizard — a multi-part loadout pays the full search → pick → scroll → qty → "+ Add" cost per part (~6 interactions, 2 keyboard switches, 2-3 scrolls each; an 8-part morning loadout ≈ 60+ touches). Ranked pain points: (1) **serial add loop** — no multi-select; the manager's `RecordMovementSheet` already has the better pattern (checkbox results → "＋ Add N" → inline qty per line) while the crew sheet, used far more and on phones, doesn't; (2) **qty field hides** — renders below the still-open ≤320px results list, off-screen on a phone, nothing scrolls it into view; (3) **cart lines aren't editable** — qty typo = × remove + full redo; (4) **by-location mode locks the location once the cart is non-empty** (`Change` disabled, :775-778) so a cross-bin loadout can't finish in one submit in that mode; (5) find-a-part demands a bin decision per part even when the part lives in one location; (6) no recents/habit memory (crews load the same 10-15 SKUs daily; recent loads derivable from `inventory_movements` by `created_by`) and no kit/assembly loadout; (7) submit loops `record_crew_movement` sequentially — 8 parts = 8 round trips on field LTE; (8) no scan mode (#13, separate item). **Fix order (settled with owner): (1) multi-select search + inline cart quantities** (adopt the manager-sheet pattern; kills pains 1-3), **(2) auto-pick source when a part has exactly one stocked location** (+ chip to override), **(3) unlock cross-bin carts in by-location LOAD mode** — per-line `otherLocationId` snapshots already support it (the lock's misroute rationale, :474, is real only for RETURN's shared destination: keep the lock there or re-point all lines on change). Recents row (4) nice-to-have; scan mode stays #13. **Blast radius (checked): small.** `CrewMovementSheet` has exactly ONE consumer — `MyStockView.jsx:320` (Load + Return + prefill-return); shared helpers (`getAllStockGrouped`/`groupLocationsByAisle`/`getMyAllowedLoadDestinations`/`matchesAllTokens`) are consumed read-only, signatures untouched; server side (`record_crew_movement` RPC, permission + whitelist checks) unchanged. **Must-not-break list:** single-part → own-truck fast path (no Add, no confirm), prefill-return from My Stock, warn-but-allow over-draw + forced review (#17), per-line source snapshots (the 8/3 Cody's-truck fix), whitelist badge-and-grey, review step triggers (return / multi / non-truck dest / over-draw), failed-lines-stay-for-retry, `useBackClose` dirty confirm, full en/es i18n (sheet is wired via `t()`; new strings need both languages — note backlog #35's "CrewMovementSheet has zero t() wiring" is stale on this point). QA personas that exercise Load (qa-harness) may need expectation updates.

--- *Items 41–51 from the Aug 15 2026 fine-tooth-comb code audit (full read of `lib/inventory.js`, `lib/supabase.js`, `AppContext`, `Login`, `admin.js`, `access.js`, `cycleCount.js`, `useCsvImport.js`, both import sheets, Crew/Record/Receive/Reconcile/Sage sheets, `TaskWorkspace` submit path, `SubmissionsQueue`, `ReportsView` data paths, `sonar-webhook`; row counts + advisor scan verified against the live DB).* ---

41. ✅ ~~**🔴 NEAR-TERM — double-import guard is unpaginated and about to cross the 1,000-row cap.**~~ — fixed Aug 15 2026 (same audit branch, commit `51ca296`): the marker query now pages via `fetchAllRows` with a stable `(created_at, id)` order; failure semantics unchanged (warn-only, keep previous set). Original finding: `useAlreadyImportedMarkers` (`lib/useCsvImport.js:244-250`) fetched `[sonar:]`/`[sonar_jobs:]` marker rows with a plain `.select('notes').like(...).gte(created_at, 90d)` — no paging, no `.order()`. PostgREST silently caps the response at 1,000 rows (the exact #29 failure class), and the guard **fails open**: lost markers read as "not yet imported," so a re-delivered Sonar report double-books consumption transfers with no error anywhere. Verified live Aug 15 2026: **778 `[sonar:]` rows already inside the 90-day window** (plus 258 `[sonar_jobs:]`), growing with every daily delivery — weeks from the cliff. Fix: route through `fetchAllRows` with a stable `(created_at, id)` order (both marker families; the fix is one query-builder change), or replace the notes-scan with a proper indexed marker column later. Add a test asserting the query pages (or a maxRows guard that throws rather than truncates).

42. ✅ ~~**🔴 Silent-failure DB writes — supabase-js doesn't throw, and several handlers never check `{error}`.**~~ — fixed Aug 15 2026: `must()` is now a shared export from `lib/supabase.js` and every site listed below goes through it (ProjectManager delete-task chain + 3 field editors, AdminPanel renames, SubmissionsQueue flag/archive, `deleteAssembly`'s first delete); AdminPanel's local copy was replaced by the shared one. The sweep rule is now documented on the helper itself. Original finding: The July gotcha ("AdminPanel's delete chains now surface the real error") was fixed in AdminPanel's *delete* chains only; the same pattern survives elsewhere and each one can toast success while the DB did nothing:
    - `ProjectManager.handleDeleteTask` (:463-470) — the whole entry_parts → log_entries → submissions → work_sessions → tasks delete chain ignores every `{error}`. Worst case is real: `inventory_movements.submission_id` / auto-deduct rows FK-block the submissions delete for any approved task, so the chain half-completes and still toasts "Task deleted" while the task remains. Port AdminPanel's `must()` wrapper.
    - `AdminPanel` rename handlers (:39 project, :52 phase) — `update` unchecked; a rename that fails RLS or FK just quietly doesn't happen.
    - `ProjectManager` field editors (:658 permit_url, :683 phase fields, :710 project fields) — same.
    - `SubmissionsQueue.handleFlag` (:259) and `handleArchive` (:353) — the submissions update itself is unchecked; a failed flag still toasts "Flagged" and clears the detail view, so the manager believes the passdown was sent back when it's still pending.
    - `supabase.js deleteAssembly` (:483) — first delete (assembly_parts) unchecked.
    Sweep rule going forward (add to conventions): every `db.from(...).update/delete/insert` that a toast or state change depends on must check `{error}` (or use a `must()` helper).

43. ✅ ~~**Reports Materials view counts other passdowns' parts (session-scoped, pre-#2 logic).**~~ — fixed Aug 15 2026: both `ReportsView.load()` and `generateProjectReport`'s parts/footage breakdown now attribute entries by `log_entries.submission_id` (direct link wins; legacy unlinked rows fall back to their session's SINGLE approved submission, skipped otherwise — same rule as getTaskSummary/SubmissionsQueue). Live-data note at fix time: the 3 legacy unlinked entries all sit in multi-submission sessions and are now excluded from Reports display (deliberate, "better absent than double-counted" — approve_submission's deduction fallback is unaffected). Original finding: `ReportsView.load()` (:203-263) and `generateProjectReport`'s parts/footage breakdowns (:457-497) fetch `log_entries` by `session_id` and ignore `log_entries.submission_id` entirely. Under the is_closed multi-passdown model a session can hold approved + pending + flagged submissions at once — so the approved-only report silently includes a pending/flagged passdown's parts (and the session→submission map at :207 is last-write-wins when two approved subs share a session, misattributing rows). `SubmissionsQueue.loadPartsForSubmission` (:200-204) and `getTaskSummary` already do this right: prefer entries linked by `submission_id`, fall back to session+task only for legacy unlinked rows. Re-key both ReportsView paths the same way. (Consumption mode is unaffected — it reads the movement ledger.)

44. ✅ ~~**PostgREST 1,000-row growth watch — the remaining unpaginated reads.**~~ — fixed Aug 15 2026: `fetchAllRows` moved to `lib/supabase.js` (re-exported from `lib/inventory.js` so existing imports keep working) and every flagged read now pages with a stable order: `getPartsCatalogIndex` (the CSV importer's SKU matcher), `getPartsCatalogTaxonomy`, `getAllStockGrouped`'s parts + locations legs, `getFullTree`/`getInfraTree`'s every-task-ever pulls, SonarImportSheet's + FiberJobsImportSheet's active-parts lookups, and ReportsView's approved-submissions fetch. The bigger server-filter for old closed tasks stays a future option; paging keeps the tree correct meanwhile. Original finding: #29 fixed the stock reads; these are the same class, currently under the cap but growing (live counts Aug 15 2026 in parens). None need fixing today; each needs `fetchAllRows` + stable order *before* its table crosses ~900:
    - `parts_catalog` full reads (709 total / 608 active): `getPartsCatalogIndex` (**feeds the CSV importer's SKU matching — silent truncation would mint duplicate drafts**), `getAllParts` is already paged, but `getPartsCatalogTaxonomy`, `getAllStockGrouped`'s parts+locations legs, SonarImportSheet's (:179) and FiberJobsImportSheet's (:124) active-parts lookups (feed part auto-matching) are not.
    - `tasks` full reads (43 now, but **unbounded — nothing ever filters or archives closed tasks out**): `getFullTree()` / `getInfraTree()` select every task ever with no server filter; the project tree will silently truncate at 1,000. Cheap forward fix when volume grows: server-filter to `is_closed=false OR closed_at > now()-interval '90 days'` with a separate fetch for the crew "Past tasks" toggle.
    - `submissions` approved-report fetch in `ReportsView.load()` (36 now) and `getTaskSummary`'s per-task submissions read — fine for long while, listed for the sweep.

45. ✅ ~~**UTC-date stamps roll over at ~5-6pm Utah time.**~~ — fixed Aug 15 2026: `isoLocalDate()` moved from SageExportSheet into `lib/format.js` and used at every stamp site — `startSession`'s `session_date`, PR `date_requested` (lib default + sheet state), Reconcile/InventoryImport sweep note stamps, ReportsView date-filter defaults/presets, and CSV filename stamps. **DB half applied live**: `crew_activity_today`'s `CURRENT_DATE` filter (server UTC — it agreed with the OLD bug, so it had to move in lockstep) is now `(now() AT TIME ZONE 'America/Denver')::date` (migration `20260815120000_crew_activity_today_local_day_invoker.sql`, checked into `supabase/migrations/`). Transition note: at most one boundary evening's sessions keep their old UTC date — harmless, the per-task upsert just creates a fresh row next day. Original finding: Several "today" stamps use `new Date().toISOString().slice(0,10)` / `.split('T')[0]`, which is the **UTC** date — after 5pm MST / 6pm MDT they produce *tomorrow*: (a) `startSession` (`lib/supabase.js:526`) — `work_sessions.session_date`, so an evening crew session lands on tomorrow's date, splitting the one-session-per-(user, day, task) model at the 6pm boundary and shifting `crew_activity_today` + same-day-passdown semantics; (b) `createPurchaseRequest`'s `date_requested` default (`lib/inventory.js:2394`) and `PurchaseRequestSheet`'s initial state (:43); (c) `ReconcileSheet`'s note stamp (:218 — "Reconcile 2026-08-16" written on the evening of the 15th); CSV filename stamps (cosmetic). `SageExportSheet` already has the correct helper (`isoLocalDate`, :331) — move it to a lib (`lib/format.js` is the natural home) and use it at all of these call sites.

46. ✅ ~~**`crew_activity_today` view lints as SECURITY DEFINER (advisor ERROR — new since the #12 hardening).**~~ — fixed Aug 15 2026 in the same migration as #45's DB half (`20260815120000_crew_activity_today_local_day_invoker.sql`): the view is recreated `WITH (security_invoker = on)`. Advisor re-scan confirms the ERROR is gone; remaining lints all match #12's documented exceptions. Original finding: The July 2026 rewrite (one-row-per-crew LATERAL version, migration `20260706120000_work_sessions_per_task.sql`) recreated the view without `security_invoker = on`, so it enforces the creator's permissions instead of the caller's — the exact lint the #12 pass fixed on the other views. Low practical risk (it reads work_sessions/users which authenticated can read anyway) but it's the only ERROR-level advisor finding and a one-line `ALTER VIEW public.crew_activity_today SET (security_invoker = on);` migration clears it. Everything else in the Aug 15 2026 security scan matches #12's documented exceptions.

47. ✅ ~~**Purchase-request lifecycle hardening (3 small holes in `lib/inventory.js`).**~~ — fixed Aug 15 2026: (a) `createPurchaseRequest` builds + validates the full lines payload before allocating a pr_number or inserting the header — no more orphaned line-less PRs; (b) new **`receive_pr_line(p_line_id, p_quantity, p_part_id)` RPC** (migration `20260815130000_receive_pr_line_rpc.sql`, applied live): staff-gated, row-locks the line + header, and does movement insert → received_qty advance → header-status recompute (partial/received) in ONE transaction — kills both the mid-sequence-failure double-book and the concurrent-receive race; the JS wrapper is now a thin RPC call (createdBy kept in the signature but unused — created_by comes from auth.uid()); smoke-tested live in rolled-back transactions (partial → received flow, movement fields, over-receive rejected). ⚠ Add a `receive_pr_line` row to docs/DB_RPCS.md once the unpushed CLAUDE.md-diet commit (590c7c9, local-only) lands — the file doesn't exist on origin yet. (c) `deletePurchaseRequest` refuses once any line has `received_qty > 0` (receipts reference the PR) — cancel instead. Original finding: (a) `createPurchaseRequest` inserts the PR header *before* validating lines (:2404-2419) — a bad line (blank description / zero qty) throws after the header landed, orphaning a numbered, line-less PR in the list and burning a `pr_number`. Validate the lines payload first (the checks are pure). (b) `receivePurchaseRequestLine` is a non-atomic read-modify-write: the receive movement books (:2611) *before* `received_qty` advances (:2625) — a failure between the two leaves stock received but the line still "outstanding," and re-receiving double-books; two managers receiving the same line concurrently both pass the `remaining` check. Fine at current scale, but the partial-receive UI (see PR-partial-receive memory) multiplies exposure — consider folding movement+counter into an RPC when that ships. (c) `deletePurchaseRequest` has no status guard — a `received` PR can be deleted, orphaning the "Received from PR-x" notes on its movements; block delete (or confirm loudly) once any line has `received_qty > 0`.

48. ✅ ~~**Sage export batch-stamp is non-atomic + preview date column misleads.**~~ — fixed Aug 15 2026: (a) a mid-chunk stamp failure now throws a self-explanatory error naming the batch id, the stamped/total count, and the exact recovery path (re-run with "Include already exported" OFF → the next batch is precisely the unstamped remainder; deliver only if the first file wasn't imported into Sage); (b) the preview table's Date column renders `movementEffectiveDate` — the same date the CSV writes. Original finding: (a) `markMovementsExported` (`lib/inventory.js:2292-2319`) inserts the batch row then stamps movements in chunks of 100 — a failure mid-loop leaves a batch claiming N movements with only some stamped; the unstamped rows re-export into the *next* batch → duplicate lines delivered to Sage across two files with different batch ids. Cheap hardening: on chunk failure, surface "batch <id> partially stamped — re-run with 'Include already exported' OFF re-exports the unstamped remainder" (or move the stamp into a single RPC). (b) `SageExportSheet`'s preview table Date column (:263) shows `created_at` while the CSV writes the effective work date (`occurred_at ?? created_at`) — for imported rows the screen and the delivered file disagree; render `movementEffectiveDate(m)` instead.

49. ✅ ~~**Receive PO: failed-submit retry trap on NEW parts + stale unit on movement rows.**~~ — fixed Aug 15 2026: (a) duplicate-key on a NEW line's `createPart` is treated as "already created — continue" (retry after a failed submit just works; also covers typing an existing SKU as new); (b) movement rows record `pendingAttrs?.unit || part.unit` so an attrs-edited unit matches the catalog update. Original finding: (a) `handleSubmit` (`ReceivePOSheet.jsx:86-106`) creates brand-new parts first; if the later movement batch fails, retry re-runs `createPart` for the same SKUs → 23505 aborts the whole submit and there's no path forward except manually re-picking each NEW part from search. Treat duplicate-key on an `isNew` line as "already created — continue" (or flip `isNew` off after a successful create). (b) The movement payload's `unit` (:127) reads `l.part.unit`, ignoring `l.pendingAttrs?.unit` — an attrs-edited unit updates the catalog but the receive row records the old unit.

50. ✅ ~~**`sonar_pending_imports` has no retention — raw_csv blobs accumulate forever.**~~ — fixed Aug 15 2026 (migration `20260815140000_sonar_pending_imports_retention.sql`, applied live): pg_cron enabled + weekly job `purge_sonar_raw_csv` (Mondays 09:30 UTC) blanks `raw_csv` on imported/discarded rows older than 60 days; pending rows are never touched (they're the only ones `loadPending` reads). One-time catch-up blanked 29 rows at apply time; job + purge verified live. Original finding: Every webhook delivery stores the full CSV in `raw_csv` (150 rows Aug 2026, 2 reports × daily, plus discarded test-fires). Nothing ever prunes them; the audit-list queries deliberately exclude the blob but the table still grows unbounded. Cheap fix: a periodic cleanup (pg_cron or a manual admin action) that NULLs `raw_csv` on `imported`/`discarded` rows older than ~60 days, keeping the audit metadata rows themselves.

51. ✅ ~~**Minor polish bucket (each ≤30 min, none urgent).**~~ — all five fixed Aug 15 2026: Reconcile flags duplicate (SKU, location) CSV rows as a warning status (first occurrence wins); `showToast` keeps one shared timer so quick successive toasts don't cut each other short; `staffScope(null)` fails closed (`null`, not `'full'`); Receive PO's destination list is warehouse/truck/group only (job_site + scrap excluded); realtime console chatter is behind an `RT_DEBUG` flag in `lib/supabase.js`. Original list:
    - `ReconcileSheet`: duplicate (SKU, location) rows in a hand-edited CSV each book the full variance against the same `currentSystem` → double-correction. Dedupe on parse (last row wins) or flag dupes as a warning status.
    - `AppContext.showToast`: successive toasts share no timer handle — an earlier toast's 3s timeout clears a later toast early. Keep the timer in a ref and reset it.
    - `access.js staffScope(null)` returns `'full'` (all tabs / not-limited for a null user) — inert today because callers always pass a user, but a fail-closed `null` is the safer default.
    - `ReceivePOSheet` destination list allows `job_site`, `scrap`, and trucks as receive targets (:247 only excludes vendors) — receiving new stock straight into a consumption bucket or scrap is almost certainly a mis-tap; restrict to warehouse/bin/truck/group or warn.
    - Realtime `console.log` chatter in `lib/supabase.js` (every task INSERT/UPDATE/DELETE + channel status) ships to production consoles — downgrade to a debug flag when next touching the file.

52. **Part lifecycle redesign — PARKED on branch `backlog/part-lifecycle-52`** (unmerged, commit `3864ed8`, Aug 19 2026). Parts get a real `status` column (`active | draft | retired | merged`) with `is_active` kept as a trigger-synced mirror, a `merge_part` redirect RPC (would unblock the #27 stock-holding merges), and Receive-PO inline-create hardening (duplicate / near-duplicate SKU detection). Full 6-phase design in `~/.claude/plans/i-want-to-look-mutable-meadow.md`; the branch's own BACKLOG.md carries the long-form entry. Pick up by merging the branch and following the plan.

53. ✅ ~~**PO receiving: Receiving-dock default + reverse a mis-receipt ("credit it back to the PO").**~~ — built Aug 20 2026 (migration `20260822120000_pr_receipt_reversal.sql`, DB live). (a) Create-PO / PR "Deliver to", the PR receive panel's bin picker, and the manual Receive PO sheet all default to the **Receiving dock** bin (resolved by name via `RECEIVING_BIN_NAME` / `getDefaultReceivingLocation()` in `lib/inventory.js` — same precedent as the field-return quarantine bin; rename the bin and the default quietly turns off). (b) Each received PR line offers **↩ Reverse**: `reverse_pr_line_receipt` RPC books an `adjust` OUT of the Deliver-to/bin (blocked if fewer on hand than requested — best-effort, the stock row isn't locked), lowers `received_qty`, recomputes status (all lines back to 0 → `ordered`, which re-unlocks the lines for a wrong-part fix), reason required into notes; Activity labels it "Receipt reversal". New `inventory_movements.purchase_request_line_id` provenance link (stamped by receive + reversal) powers the "received into <bin>" readout and the reversal's default source. Sage-neutral: receives and adjusts both never export. **Follow-ups:** an admin-set default bin instead of the name match if the dock gets renamed/split; reversals for field-return / found receipts are deliberately out of scope (no PO to credit).

54. ✅ ~~**Purchasing (PRs & POs) hidden behind an Admin switch.**~~ — shipped Aug 20 2026. Owner decision: purchase requests / POs will not live in FiberLog for now, but the code stays. `app_settings.purchasing_ui_enabled` (`'on' | 'off'`, seeded **off**; migration `20260823120000_purchasing_ui_flag.sql` widens the two key-scoped `app_settings` policies to the new key — the table also holds `boxhero_api_key`, so never open it). `AppContext.purchasingEnabled` (fail-closed: missing row / read error = off, realtime on its own channel). OFF hides: the Purchase Reqs sub-tab, **＋ New PR / ＋ New PO**, the "Receiving against a PO? / ＋ Create a PO" block in Receive PO (both hand-off props withheld, so the sheet doesn't even query open POs), and the Create-PR bulk buttons in Stock / Parts. NOT behind the switch: manual Receive PO (incl. inline part create with Sage ID) and Returned from field. The 5 entered POs (PR-2026-0011…0015) stay in the DB and reappear when **Admin → Purchasing (PRs & POs)** is turned On — live, no deploy.
